function calculateCylinderVolume(side) {
  let result = 4 * side;
  console.log(
    `Hasil keliling dari persegi yang memiliki sisi berukuran ${side} adalah ${result}`
  );
}

calculateCylinderVolume(25);
