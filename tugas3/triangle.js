function calculateTriangle(base, height) {
    let result = 1/2 * base * height
    console.log(`Hasil luas dari segitiga dengan alas = ${base} dan tinggi ${height} adalah ${result}`)
}

calculateTriangle(20, 15)