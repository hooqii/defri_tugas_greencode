function calculateModulo(firstNumber, secondNumber) {
    let result = firstNumber % secondNumber 
    console.log(`Hasil dari sisa bagi dari ${firstNumber} dan ${secondNumber} adalah ${result}`)
}

calculateModulo(10, 3)