const increment = (x, y) => x + y;
const decrement = (x, y) => x - y;
const multiply = (x, y) => x * y;
const distribution = (x, y) => x / y;

module.exports = { increment, decrement, multiply, distribution };